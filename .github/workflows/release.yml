name: Test & Release

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Test
      run: dotnet test --configuration Release --verbosity normal
      
    - name: Pack
      run: |
        dotnet pack --no-restore --configuration Release
        ls
        cd src
        ls
        cd bin
        ls
        cd Release
        ls
      
    - name: Publish
      run: |
        dotnet nuget add source --username percyqaz --password ${{ secrets.PACKAGES_TOKEN }} --store-password-in-clear-text --name github "https://nuget.pkg.github.com/percyqaz/index.json"
        dotnet nuget push "src/bin/Release/Percyqaz.Shell.3.3.0.nupkg" --api-key ${{ secrets.PACKAGES_TOKEN }} --source "github"
